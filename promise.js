// const user = 'ramesh';
// const password = 'password';

let promise = new Promise((res, rej) => {
    if(user ===user && Password === password){
        res(console.log('user is logged in'))
    }

    else{
        rej(console.log('authentication failed'))
    }

})